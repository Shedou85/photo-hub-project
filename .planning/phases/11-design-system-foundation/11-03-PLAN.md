---
phase: 11-design-system-foundation
plan: 03
type: execute
wave: 2
depends_on: ["11-01"]
files_modified:
  - frontend/src/pages/HomePage.jsx
  - frontend/src/pages/LoginPage.jsx
  - frontend/src/pages/SharePage.jsx
  - frontend/src/pages/DeliveryPage.jsx
autonomous: true

must_haves:
  truths:
    - "All hardcoded hex background colors in public pages replaced with semantic color tokens or Tailwind palette"
    - "All hardcoded shadow-[...] replaced with shadow token classes"
    - "All hardcoded rounded-[10px] replaced with rounded (DEFAULT) or appropriate token"
    - "All hardcoded text-[Npx] replaced with text-xs/sm/base/lg/xl where they match the Major Third scale"
    - "No functional or visual regressions in public/client-facing pages"
    - "Build succeeds and lint passes with zero warnings"
  artifacts:
    - path: "frontend/src/pages/HomePage.jsx"
      provides: "Token-based landing page styling"
    - path: "frontend/src/pages/LoginPage.jsx"
      provides: "Token-based login page styling"
    - path: "frontend/src/pages/SharePage.jsx"
      provides: "Token-based share/selection page styling"
    - path: "frontend/src/pages/DeliveryPage.jsx"
      provides: "Token-based delivery page styling"
  key_links: []
---

<objective>
Refactor all public/client-facing pages (HomePage, LoginPage, SharePage, DeliveryPage) to use design tokens, then verify the entire codebase has no remaining hardcoded values that should be tokens.

Purpose: Complete the design system migration for all remaining pages. After this plan, every component in the codebase uses the centralized token system, satisfying DESIGN-07 (no hardcoded values in components).

Output: Four refactored page files. All component files in the codebase use design tokens.
</objective>

<execution_context>
@C:/Users/Marius/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Marius/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-design-system-foundation/11-01-SUMMARY.md
@frontend/tailwind.config.js
@frontend/src/constants/breakpoints.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor HomePage and LoginPage to use design tokens</name>
  <files>
    frontend/src/pages/HomePage.jsx
    frontend/src/pages/LoginPage.jsx
  </files>
  <action>
These two pages share a dark theme with `#0d0f14` background, `#1a1f35` dropdown backgrounds, and common patterns.

**HomePage.jsx refactoring:**

1. **Background colors:**
   - `bg-[#0d0f14]` → `bg-surface-darker` (3 occurrences: page bg, hero section, pricing section)
   - `bg-[#f5f6fa]` → `bg-surface-light` (features section)
   - `bg-[#080a0f]` → `bg-surface-darkest` (footer)
   - `bg-[#1a1f35]` → `bg-surface-dark-alt` (language dropdown)
   - `bg-[#0d0f14]/90` → `bg-surface-darker/90` (sticky navbar)

2. **Font sizes:**
   - `text-[15px]` → `text-base` (16px — 1px larger, acceptable for consistency)
   - `text-[16px]` → `text-base` (exact match)
   - `text-[13px]` → `text-sm` (14px — 1px larger)
   - `text-[22px]` → `text-xl` (22px = xl token)
   - `text-[28px]` → `text-2xl` (28px = 2xl token)
   - `text-[42px]` → `text-4xl` (48px — visually close; or keep as `text-[42px]` since it's a pricing display). Keep as `text-[42px]` — pricing hero number has specific visual weight.
   - `text-[14px]` → `text-sm` (14px = sm token)
   - `text-[11px]` → `text-xs` (12px)
   - `text-[12px]` → `text-xs` (12px = xs token)
   - `text-[9px]` → keep as `text-[9px]` (dashboard preview badges, intentionally tiny)
   - `text-[10px]` → keep as `text-[10px]` (dropdown caret, language selector)

3. **Border radius:**
   - `rounded-[14px]` → `rounded-lg` (16px — 2px larger, acceptable for feature cards)
   - `rounded-[16px]` → `rounded-lg` (16px = lg token, exact match)
   - `rounded-[10px]` → `rounded` (DEFAULT = 10px)
   - `rounded-[8px]` → `rounded-sm` (8px = sm token, exact match)
   - `rounded-[12px]` → `rounded-md` (12px = md token, exact match)

4. **Shadows:**
   - `shadow-[0_4px_24px_rgba(0,0,0,0.4)]` → `shadow-xl` (token: 16px 48px 0.16 — different but semantic elevation is close; for dark mode the exact shadow is less visible anyway)
   - `shadow-[0_2px_12px_rgba(99,102,241,0.35)]` → keep as-is (brand-colored shadow, no token equivalent)
   - `shadow-[0_4px_16px_rgba(99,102,241,0.35)]` → keep as-is (brand-colored shadow)
   - `shadow-[0_4px_16px_rgba(99,102,241,0.4)]` → keep as-is (brand-colored shadow)
   - `shadow-[0_4px_20px_rgba(99,102,241,0.4)]` → keep as-is (brand-colored shadow)
   - `shadow-[0_0_40px_rgba(99,102,241,0.25)]` → keep as-is (pricing card glow)
   - `shadow-[0_8px_24px_rgba(0,0,0,0.5)]` → `shadow-lg` (close enough for dark dropdown)
   - `shadow-[inset_0_1px_0_rgba(255,255,255,...)]` → keep as-is (inset glass effect, unique)

5. **Spacing:**
   - `tracking-[0.15em]` → keep as-is (hero subtitle tracking)

6. **Viewport-relative / blur:**
   - `w-[600px]`, `h-[600px]`, `w-[400px]`, `h-[400px]`, `w-[800px]`, `h-[300px]` → keep as-is (background blur decoration, context-specific)
   - `blur-[120px]`, `blur-[100px]` → keep as-is (background effect)
   - `max-w-[420px]` → keep as-is (hero text max-width, context-specific)

7. **Gradients:** Keep ALL `bg-[linear-gradient(...)]` as-is (legitimate Tailwind v3 pattern).

8. **Opacity modifiers:** `border-white/[0.07]`, `border-white/[0.05]`, `bg-indigo-600/[0.08]`, `bg-blue-500/[0.06]`, `opacity-[0.07]`, `bg-white/[0.04]`, `bg-white/[0.09]`, etc. — keep as-is; these are Tailwind arbitrary opacity modifiers, the correct syntax for custom opacities.

**LoginPage.jsx refactoring:**

1. **Background colors:**
   - `bg-[#0d0f14]` → `bg-surface-darker` (page bg)
   - `bg-[#0d0f14]/90` → `bg-surface-darker/90` (sticky navbar)
   - `bg-[#1a1f35]` → `bg-surface-dark-alt` (language dropdown)

2. **Font sizes:**
   - `text-[15px]` → `text-base` (16px)
   - `text-[13px]` → `text-sm` (14px)
   - `text-[14px]` → `text-sm` (14px = sm)
   - `text-[28px]` → `text-2xl` (28px = 2xl)
   - `text-[10px]` → keep as-is (dropdown caret)

3. **Border radius:**
   - `rounded-[16px]` → `rounded-lg` (16px = lg token)
   - `rounded-[10px]` → `rounded` (DEFAULT)
   - `rounded-[8px]` → `rounded-sm` (8px = sm token)

4. **Shadows:**
   - `shadow-[0_4px_24px_rgba(0,0,0,0.4)]` → `shadow-xl`
   - `shadow-[0_0_40px_rgba(0,0,0,0.4)]` → `shadow-xl` (card shadow)
   - `shadow-[0_8px_24px_rgba(0,0,0,0.5)]` → `shadow-lg` (dropdown)
   - `shadow-[0_4px_16px_rgba(99,102,241,0.35)]` → keep as-is (brand-colored button shadow)

5. **Spacing:**
   - `mb-[5px]` → `mb-1` (4px)

6. **Opacity modifiers:** Keep `bg-white/[0.06]`, `border-white/[0.12]`, `bg-white/[0.08]` etc. as-is.

7. **Decorative elements:**
   - `w-[600px] h-[600px]` → keep as-is (background glow circle, context-specific)
   - `max-w-[420px]` → keep as-is (login card max-width)
   - `opacity-[0.07]` → keep as-is (glow circle opacity)

8. **Gradients:** Keep all `bg-[linear-gradient(...)]` as-is.
  </action>
  <verify>
1. Run `npm run dev` — no compilation errors
2. Run `npm run lint` — zero warnings
3. Grep `bg-[#0d0f14]` across both files — should find 0 (replaced with bg-surface-darker)
4. Grep `bg-[#1a1f35]` across both files — should find 0 (replaced with bg-surface-dark-alt)
5. Grep `bg-[#080a0f]` in HomePage — should find 0 (replaced with bg-surface-darkest)
6. Grep `bg-[#f5f6fa]` in HomePage — should find 0 (replaced with bg-surface-light)
7. Grep `rounded-[10px]` across both files — should find 0
  </verify>
  <done>
- HomePage and LoginPage use semantic color tokens (surface-darker, surface-light, surface-darkest, surface-dark-alt)
- Font sizes use Major Third scale tokens (text-base, text-sm, text-xl, text-2xl, text-xs)
- Border radius uses token classes (rounded, rounded-sm, rounded-md, rounded-lg)
- Standard shadows use token classes (shadow-lg, shadow-xl)
- Brand-colored shadows, gradients, opacity modifiers, and viewport-relative decorations kept as legitimate arbitrary values
  </done>
</task>

<task type="auto">
  <name>Task 2: Refactor SharePage and DeliveryPage, then verify entire codebase</name>
  <files>
    frontend/src/pages/SharePage.jsx
    frontend/src/pages/DeliveryPage.jsx
  </files>
  <action>
**SharePage.jsx refactoring:**

1. **Font sizes:**
   - `text-[48px]` → `text-4xl` (48px = 4xl token, exact match)
   - `text-[28px]` → `text-2xl` (28px = 2xl token)

2. **Border radius:**
   - `rounded-[6px]` → `rounded-sm` (8px)
   - `rounded-[10px]` → `rounded` (DEFAULT)
   - `rounded-[4px]` → keep as `rounded-[4px]` (lightbox image, minimal corners)

3. **Shadows:**
   - `shadow-[0_-4px_12px_rgba(0,0,0,0.08)]` → keep as-is (upward-facing shadow for fixed bottom bar, directional shadow not matched by any token)

4. **Spacing:**
   - `max-w-[480px]` → keep as-is (loading state width, context-specific)
   - `max-w-[720px]` → keep as-is (page width, context-specific)
   - `py-[14px]` → `py-3.5` (14px = Tailwind 3.5)
   - `max-w-[88vw] max-h-[88vh]` → keep as-is (viewport-relative lightbox)

5. **Gradients:** Keep all `bg-[linear-gradient(...)]` as-is.

6. **Button patterns:**
   - `rounded-[10px]` → `rounded` (DEFAULT)
   - `hover:scale-[1.02]` → keep as-is (micro-interaction, custom scale)
   - `active:scale-[0.98]` → keep as-is (micro-interaction)

**DeliveryPage.jsx refactoring:**

1. **Font sizes:**
   - `text-[48px]` → `text-4xl` (48px = 4xl token)
   - `text-[28px]` → `text-2xl` (28px = 2xl token)

2. **Border radius:**
   - `rounded-[6px]` → `rounded-sm` (8px)
   - `rounded-[10px]` → `rounded` (DEFAULT)
   - `rounded-[4px]` → keep as `rounded-[4px]` (lightbox image)

3. **Spacing:**
   - `max-w-[480px]` → keep as-is (loading state width)
   - `max-w-[720px]` → keep as-is (page width)
   - `py-[14px]` → `py-3.5` (14px = Tailwind 3.5)
   - `max-w-[88vw] max-h-[88vh]` → keep as-is (viewport-relative lightbox)

4. **Button patterns:**
   - `rounded-[10px]` → `rounded`
   - `hover:scale-[1.02]` → keep as-is
   - `active:scale-[0.98]` → keep as-is

5. **Gradients:** Keep all `bg-[linear-gradient(...)]` as-is.

**Final codebase verification (after both files refactored):**

Run these verification commands to confirm no stale hardcoded values remain:

```bash
# Should find 0: hardcoded hex bg colors (excluding gradients)
grep -rn "bg-\[#" frontend/src/ --include="*.jsx" --include="*.js" | grep -v "linear-gradient"

# Should find 0: rounded-[10px]
grep -rn "rounded-\[10px\]" frontend/src/ --include="*.jsx"

# Should find 0: shadow-[ (excluding brand-colored and directional shadows)
# Acceptable remaining: shadow-[inset_...] (glass effects), shadow-[0_-4px...] (upward), shadow-[0_Npx_Npx_rgba(99,102,241,...)] (brand-colored)
grep -rn "shadow-\[" frontend/src/ --include="*.jsx"

# Document remaining arbitrary values count
grep -rn "\-\[" frontend/src/ --include="*.jsx" | wc -l
```

Remaining arbitrary values should only be from these LEGITIMATE categories:
- **Gradients**: `bg-[linear-gradient(...)]` — Tailwind v3 has no native multi-stop gradient config
- **Brand-colored shadows**: `shadow-[0_Npx_Npx_rgba(99,102,241,...)]` — indigo glow effects
- **Directional shadows**: `shadow-[0_-4px...]` — upward shadow for fixed bottom bar
- **Inset shadows**: `shadow-[inset_...]` — glass morphism effects
- **Viewport units**: `max-w-[88vw]`, `max-h-[88vh]` — lightbox sizing
- **Page widths**: `max-w-[720px]`, `max-w-[480px]`, `max-w-[420px]` — layout constraints
- **Decorative sizes**: `w-[600px]`, `h-[600px]` etc. — background blur decorations
- **Blur values**: `blur-[120px]`, `blur-[100px]` — background effects
- **Micro-interactions**: `hover:scale-[1.02]`, `active:scale-[0.98]`
- **Custom tracking**: `tracking-[0.05em]`, `tracking-[0.06em]`, `tracking-[0.15em]`, `tracking-[0.5px]`
- **Z-index stacking**: `z-[1001]`, `z-[1002]`, `z-[1003]`
- **Badge text**: `text-[9px]`, `text-[10px]` — intentionally sub-xs
- **Opacity modifiers**: `border-white/[0.07]`, `bg-white/[0.04]` etc. — Tailwind syntax
- **Grid columns**: `grid-cols-[repeat(auto-fill,...)]`
- **Font import**: `font-['Outfit',sans-serif]` — page-specific font override
- **Max heights**: `max-h-[500px]` — accordion animation
- **Custom border**: `border-l-[3px]`, `border-[1.5px]`
- **Lightbox corners**: `rounded-[4px]` — minimal radius
- **Pricing number**: `text-[42px]` — specific visual weight
  </action>
  <verify>
1. Run `npm run dev` — no compilation errors
2. Run `npm run build` — production build succeeds with no errors
3. Run `npm run lint` — zero warnings
4. Grep `rounded-[10px]` across ALL frontend/src/ — should find 0
5. Grep `bg-[#` across ALL frontend/src/ (excluding gradients) — should find 0
6. Grep `text-[22px]` across ALL frontend/src/ — should find 0 (replaced with text-xl)
7. Grep `text-[28px]` across ALL frontend/src/ — should find 0 (replaced with text-2xl)
  </verify>
  <done>
- SharePage and DeliveryPage use design token classes
- Zero hardcoded hex background colors remain in entire codebase (excluding gradient syntax)
- Zero hardcoded rounded-[10px] remain in entire codebase
- Zero hardcoded text-[22px] or text-[28px] remain (all use text-xl/text-2xl)
- All remaining arbitrary values are documented as legitimate (gradients, brand shadows, viewport units, etc.)
- Build and lint pass
  </done>
</task>

</tasks>

<verification>
1. `npm run dev` starts without errors
2. `npm run build` succeeds
3. `npm run lint` passes with zero warnings
4. Codebase-wide checks:
   - `bg-[#` with no `linear-gradient` → 0 matches
   - `rounded-[10px]` → 0 matches
   - `text-[22px]` → 0 matches
   - `text-[28px]` → 0 matches
5. All remaining arbitrary values fall into documented legitimate categories
</verification>

<success_criteria>
- Four public/client-facing pages refactored to use design tokens
- Zero hardcoded hex colors remain across entire codebase (except inside gradient syntax)
- Zero hardcoded rounded-[10px] remain anywhere
- Zero hardcoded shadow-[...] for standard neutral shadows
- All text sizes use Major Third scale tokens where applicable
- All remaining arbitrary values documented as legitimate exceptions
- DESIGN-07 satisfied: no hardcoded values that should be tokens
</success_criteria>

<output>
After completion, create `.planning/phases/11-design-system-foundation/11-03-SUMMARY.md`
</output>
