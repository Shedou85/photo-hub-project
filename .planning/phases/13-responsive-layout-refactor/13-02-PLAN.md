---
phase: 13-responsive-layout-refactor
plan: 02
type: execute
wave: 2
depends_on: ["13-01"]
files_modified:
  - frontend/src/pages/CollectionDetailsPage.jsx
  - frontend/src/pages/CollectionsListPage.jsx
  - frontend/src/pages/ProfilePage.jsx
  - frontend/src/pages/PaymentsPage.jsx
  - frontend/src/pages/SharePage.jsx
  - frontend/src/pages/DeliveryPage.jsx
autonomous: true

must_haves:
  truths:
    - "Photo grids scale responsively: 1-col on mobile (<768px), 2-col on tablet (768-1023px), 3-col on desktop (>=1024px)"
    - "All authenticated pages have max-w-6xl (1152px) container to prevent ultra-wide sprawl"
    - "Client-facing pages (SharePage, DeliveryPage) have responsive photo grids"
    - "Layout works at in-between sizes (800px, 1100px, 1400px) without overflow or broken grids"
  artifacts:
    - path: "frontend/src/pages/CollectionDetailsPage.jsx"
      provides: "Responsive photo grids with 1/2/3 column scaling"
      contains: "grid-cols-1 md:grid-cols-2 lg:grid-cols-3"
    - path: "frontend/src/pages/CollectionsListPage.jsx"
      provides: "Responsive collection card grid with max-w-6xl container"
      contains: "max-w-6xl"
    - path: "frontend/src/pages/SharePage.jsx"
      provides: "Responsive client-facing photo grid"
      contains: "grid-cols-1 md:grid-cols-2 lg:grid-cols-3"
    - path: "frontend/src/pages/DeliveryPage.jsx"
      provides: "Responsive delivery photo grid"
      contains: "grid-cols-1 md:grid-cols-2 lg:grid-cols-3"
  key_links:
    - from: "frontend/src/pages/CollectionDetailsPage.jsx"
      to: "Tailwind responsive grid classes"
      via: "Mobile-first grid-cols with md: and lg: breakpoints"
      pattern: "grid-cols-1 md:grid-cols-2 lg:grid-cols-3"
    - from: "frontend/src/pages/CollectionsListPage.jsx"
      to: "Max-width container"
      via: "max-w-6xl mx-auto wrapper"
      pattern: "max-w-6xl mx-auto"
---

<objective>
Refactor photo grids across all pages to scale responsively (1-col mobile, 2-col tablet, 3-col desktop) and update page containers to use max-w-6xl for ultra-wide sprawl prevention.

Purpose: Complete LAYOUT-04 (responsive photo grids), LAYOUT-05 (max-width containers), and LAYOUT-06 (in-between size testing) requirements.
Output: Updated grid and container classes in 6 page files.
</objective>

<execution_context>
@C:/Users/Marius/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Marius/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-responsive-layout-refactor/13-01-SUMMARY.md

# Pages to refactor
@frontend/src/pages/CollectionDetailsPage.jsx
@frontend/src/pages/CollectionsListPage.jsx
@frontend/src/pages/ProfilePage.jsx
@frontend/src/pages/PaymentsPage.jsx
@frontend/src/pages/SharePage.jsx
@frontend/src/pages/DeliveryPage.jsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update authenticated page containers and photo grids</name>
  <files>
    frontend/src/pages/CollectionDetailsPage.jsx
    frontend/src/pages/CollectionsListPage.jsx
    frontend/src/pages/ProfilePage.jsx
    frontend/src/pages/PaymentsPage.jsx
  </files>
  <action>
Update 4 authenticated pages with responsive grids and max-width containers.

**Important context:** These pages render inside MainLayout (desktop) or MobileLayout (mobile). MainLayout already provides `py-7 px-8` padding on the `<main>` element. MobileLayout provides `p-4 pb-24` padding. Therefore, page-level containers should NOT duplicate outer padding — remove `px-6 py-7` from page wrappers since the layout already handles padding.

**1. `CollectionsListPage.jsx`:**
- Current outer wrapper: `<div className="px-6 py-7 font-sans max-w-[720px] mx-auto">`
- Change to: `<div className="font-sans max-w-6xl mx-auto">`
  - Remove `px-6 py-7` (layout provides padding)
  - Change `max-w-[720px]` to `max-w-6xl` (1152px) — wider to accommodate 3-col card grid
- Collection card grid (line 216): Change `grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6` to `grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6` — use `md:` (768px) not `sm:` (640px) for tablet breakpoint per research recommendation

**2. `CollectionDetailsPage.jsx`:**
- Loading state wrapper (line ~572): `<div className="py-7 px-6 font-sans max-w-[720px] mx-auto">` → `<div className="font-sans max-w-6xl mx-auto">`
- Main content wrapper (line ~589): `<div className="py-7 px-6 font-sans max-w-[720px] mx-auto">` → `<div className="font-sans max-w-6xl mx-auto">`
- Photo grid (line ~833): `grid grid-cols-2 sm:grid-cols-3 md:grid-cols-4 gap-2` → `grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-3`
  - 1 col on mobile for larger photo previews, 2 on tablet, 3 on desktop
  - Change `gap-2` to `gap-3` for slightly more breathing room
- Edited photos grid (line ~963): Same change as photo grid — `grid grid-cols-2 sm:grid-cols-3 md:grid-cols-4 gap-2` → `grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-3`
- Info cards grid (line ~629): `grid grid-cols-[repeat(auto-fill,minmax(180px,1fr))] gap-4` — leave as-is; auto-fill with minmax is already responsive and appropriate for info cards

**3. `ProfilePage.jsx`:**
- Current outer wrapper (line ~98): `<div className="px-6 py-7 font-sans max-w-[720px] mx-auto">`
- Change to: `<div className="font-sans max-w-6xl mx-auto">`
- Promotional photos grid (line ~184): `grid grid-cols-[repeat(auto-fill,minmax(180px,1fr))] gap-4` — leave as-is; auto-fill with minmax is already responsive

**4. `PaymentsPage.jsx`:**
- Current outer wrapper (line ~8): `<div className="py-7 px-6 font-sans max-w-[720px] mx-auto">`
- Change to: `<div className="font-sans max-w-6xl mx-auto">`

For all 4 pages: The `font-sans` class is kept for typography consistency. The `mx-auto` centers the max-width container.
  </action>
  <verify>
Run `cd frontend && npm run lint` — zero warnings.
Run `cd frontend && npm run build` — no errors.
Verify no remaining `max-w-[720px]` in modified files: `grep -c "max-w-\[720px\]" frontend/src/pages/CollectionsListPage.jsx frontend/src/pages/CollectionDetailsPage.jsx frontend/src/pages/ProfilePage.jsx frontend/src/pages/PaymentsPage.jsx` should return 0 for each file.
Verify responsive grid classes: `grep "grid-cols-1 md:grid-cols-2 lg:grid-cols-3" frontend/src/pages/CollectionDetailsPage.jsx` should return matches.
  </verify>
  <done>
All 4 authenticated pages use max-w-6xl containers. Photo grids use 1/2/3 column responsive scaling with md: and lg: breakpoints. No remaining max-w-[720px] in modified files. Page-level padding removed (handled by layout).
  </done>
</task>

<task type="auto">
  <name>Task 2: Update client-facing pages with responsive photo grids</name>
  <files>
    frontend/src/pages/SharePage.jsx
    frontend/src/pages/DeliveryPage.jsx
  </files>
  <action>
Update 2 client-facing (public) pages with responsive photo grids. These pages are NOT wrapped in MainLayout/MobileLayout — they have their own page-level containers, so keep their existing padding/max-width patterns for the overall wrapper.

**1. `SharePage.jsx`:**
- Photo grid (line ~213): `grid grid-cols-2 sm:grid-cols-3 md:grid-cols-4 gap-2 mb-10` → `grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-3 mb-10`
  - 1 col mobile for better photo viewing on small screens, 2 tablet, 3 desktop
  - Change `gap-2` to `gap-3` for consistency with authenticated pages
- Keep existing page wrapper `max-w-[720px] mx-auto py-10 px-6` — client pages are intentionally narrower for focused photo browsing experience

**2. `DeliveryPage.jsx`:**
- Photo grid (line ~162): `grid grid-cols-2 sm:grid-cols-3 md:grid-cols-4 gap-2 mb-10` → `grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-3 mb-10`
  - Same responsive pattern as SharePage for consistency
- Keep existing page wrapper — same reasoning as SharePage

Both SharePage and DeliveryPage should now match the responsive grid pattern used in authenticated pages.
  </action>
  <verify>
Run `cd frontend && npm run lint` — zero warnings.
Run `cd frontend && npm run build` — no errors.
Verify responsive grid classes: `grep "grid-cols-1 md:grid-cols-2 lg:grid-cols-3" frontend/src/pages/SharePage.jsx frontend/src/pages/DeliveryPage.jsx` should return matches for both files.
Verify no remaining old grid pattern: `grep "grid-cols-2 sm:grid-cols-3 md:grid-cols-4" frontend/src/pages/SharePage.jsx frontend/src/pages/DeliveryPage.jsx` should return 0 matches.
  </verify>
  <done>
SharePage and DeliveryPage photo grids use 1/2/3 column responsive scaling. No remaining `grid-cols-2 sm:grid-cols-3 md:grid-cols-4` patterns in either file. Client-facing pages match the authenticated pages' responsive grid behavior.
  </done>
</task>

</tasks>

<verification>
After both tasks complete:

1. **Build verification:** `cd frontend && npm run build` succeeds
2. **Lint verification:** `cd frontend && npm run lint` passes with zero warnings
3. **Responsive grids:** All photo grids across 6 pages use `grid-cols-1 md:grid-cols-2 lg:grid-cols-3` pattern
4. **Max-width containers:** All 4 authenticated pages use `max-w-6xl mx-auto`
5. **No old patterns:** No remaining `max-w-[720px]` in authenticated page wrappers; no remaining `grid-cols-2 sm:grid-cols-3 md:grid-cols-4` in photo grids
6. **Client pages preserved:** SharePage and DeliveryPage keep their own wrapper styling (max-w-[720px])
7. **Layout padding:** Authenticated page-level wrappers no longer duplicate padding (handled by layout)
</verification>

<success_criteria>
- Photo grids scale 1-col (<768px) -> 2-col (768-1023px) -> 3-col (>=1024px) across all pages
- Authenticated pages constrained to max-w-6xl (1152px) preventing ultra-wide sprawl
- No horizontal overflow at in-between viewport widths (800px, 1100px, 1400px)
- All files pass lint and build
</success_criteria>

<output>
After completion, create `.planning/phases/13-responsive-layout-refactor/13-02-SUMMARY.md`
</output>
